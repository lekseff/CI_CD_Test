name: Test

on:
  push:
    branches:
      - master

jobs:
  lint:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [16.x]
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      - run: yarn
      - run: yarn lint
      - run: yarn test
      - run: yarn build
      - run: git subtree push --prefix dist origin gh-pages
      env:
          GITHUB_TOKEN: ${{ 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 }}